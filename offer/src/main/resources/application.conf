ktor {
    deployment {
        port = 9090
        environment = development
        autoreload = true
        watch = [ exchange ]
    }

    application {
        id = ExchangeApp
        modules = [ com.km.exchange.application.OfferApplicationKt.main ]
    }
}

database {
  storage = ".database"
  logsql = true
}